/* Unit Test Runner: t-rexx */
context('Checking the FUNCNAME function')

/* Unit tests */
check("finds the largest product if span equals length"
check("can find the largest product of 2 with numbers in order"
check("can find the largest product of 2"
check("can find the largest product of 3 with numbers in order"
check("can find the largest product of 3"
check("can find the largest product of 5 with numbers in order"
check("can get the largest product of a big number"
check("reports zero if the only digits are zero"
check("reports zero if all spans include zero"
check("rejects span longer than string length"
check("reports 1 for empty string and empty product (0 span)"
check("reports 1 for nonempty string and empty product (0 span)"
check("rejects empty string and nonzero span"
check("rejects invalid character in digits"
check("rejects negative span"
check("rejects negative span"
